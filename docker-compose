version: "3.9"
services:
  express:
    build: .
    ports:
      - "3000:3000"
    links:
      - redis
    volumes:
        - type: bind
          source: ./.env
          target: /usr/.env
          read_only: true
  redis:
    image: redis:6-alpine
    ports:
        - "6379:6379"

